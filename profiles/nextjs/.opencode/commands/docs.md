---
description: Documentation updates — MkDocs pages, README, runbooks, changelogs, with company conventions.
agent: docs
---

Work on documentation for: $ARGUMENTS

Follow existing docs structure in `docs/`. Use MkDocs-compatible Markdown with YAML frontmatter (`title`, `tags`). README must include: description, setup instructions, how to run, required env vars (without values). CHANGELOG follows Keep a Changelog format or Conventional Commits auto-generation. Reference SemVer for releases. Keep docs concise, accurate, linked, and free of secrets or sensitive internal details.
